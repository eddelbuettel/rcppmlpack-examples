
// A header file name ${packagename}_types.h is, if it exists, included in the
// generated file RcppExports.cpp. This allows us to pass this #define on so
// that Armadillo 15.0.1 (or newer) is used.

#if !defined(ARMA_USE_CURRENT)
  #define ARMA_USE_CURRENT
#endif
